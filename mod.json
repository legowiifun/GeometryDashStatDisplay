{
	"geode": "5.0.0-beta.3",
	"gd": {
		"win": "2.2081",
		"android": "2.2081",
		"mac": "2.2081",
		"ios": "2.2081"
	},
	"id": "legowiifun.stat_display",
	"name": "Stat Display",
	"version": "v2.1.1",
	"developer": "legowiifun",
	"description": "Display stats while playing levels",
	"settings": {
		"enableLogs": {
			"name": "Enable debug Logs",
			"description": "Enables some debug logs created for testing.",
			"type": "bool",
			"default": false
		},
		"showPause": {
			"name": "Add a button to the pause menu",
			"description": "Adds a button to the pause menu to control the mod settings",
			"type": "bool",
			"default": false
		},
		"position": {
			"name": "Position",
			"description": "Where do you want the nodes to appear?",
			"type": "string",
			"one-of": [ "top-left", "top-right", "bottom-left", "bottom-right" ],
			"default": "top-left"
		},
		"useImg": {
			"name": "Use an image instead of text",
			"description": "Do you want to use text, or an image to display the stats?",
			"type": "bool",
			"default": false
		},
		"separator": {
			"type": "title",
			"name": "Types of stats to display"
		},
		"displayJumps": {
			"name": "Display how many jumps you have done",
			"description": "When do you want your jump count to display on the screen?",
			"type": "string",
			"one-of": [ "never", "always" ],
			"default": "never"
		},
		"displayAttempts": {
			"name": "Display total attempts",
			"description": "When do you want your attempt count to display on the screen?",
			"type": "string",
			"one-of": [ "never", "always" ],
			"default": "never"
		},
		"displayStars": {
			"name": "Display Stars",
			"description": "When do you want stars to display on the screen?",
			"type": "string",
			"one-of": [ "never", "when playing rated classic levels", "always" ],
			"default": "when playing rated classic levels"
		},
		"displayOnlineLevels": {
			"name": "Display Online Levels",
			"description": "When do you want the number of completed online levels to display on the screen?",
			"type": "string",
			"one-of": [ "never", "always" ],
			"default": "never"
		},
		"displayDemons": {
			"name": "Display your demon count",
			"description": "When do you want your completed demon count to display on the screen?",
			"type": "string",
			"one-of": [ "never", "when playing demon levels", "always" ],
			"default": "when playing demon levels"
		},
		"displayLiked": {
			"name": "Display Liked Levels",
			"description": "When do you want the number of liked levels to display on the screen?",
			"type": "string",
			"one-of": [ "never", "always" ],
			"default": "never"
		},
		"displayRated": {
			"name": "Display Rated Levels",
			"description": "When do you want the number of rated levels to display on the screen?",
			"type": "string",
			"one-of": [ "never", "always" ],
			"default": "never"
		},
		"displayCoins": {
			"name": "Display User Coins",
			"description": "When do you want user coins to display on the screen?",
			"type": "string",
			"one-of": [ "never", "when playing levels with rated coins", "always" ],
			"default": "when playing levels with rated coins"
		},
		"displayManaOrbs": {
			"name": "Display Mana Orbs",
			"description": "When do you want mana orbs to display on the screen?",
			"type": "string",
			"one-of": [ "never", "when playing rated levels", "always" ],
			"default": "never"
		},
		"displayMoons": {
			"name": "Display Moons",
			"description": "When do you want moons to display on the screen?",
			"type": "string",
			"one-of": [ "never", "when playing rated platformer levels", "always" ],
			"default": "when playing rated platformer levels"
		},
		"displayLists": {
			"name": "Display List Rewards",
			"description": "When do you want list rewards to display on the screen?",
			"type": "string",
			"one-of": [ "never", "always" ],
			"default": "never"
		},
		"displayInsanes": {
			"name": "Display Insanes",
			"description": "When do you want the number of insane levels you have completed to display on the screen?",
			"type": "string",
			"one-of": [ "never", "when playing insane difficulty levels", "always" ],
			"default": "when playing insane difficulty levels"
		}
	},
	"api": {
		"include": ["src/api.hpp"]
	},
	"tags": ["gameplay","offline","enhancement","interface","utility"],
	"dependencies": {
  		"geode.node-ids": ">=v1.22.0"
 	},
	"links": {
		"source": "https://github.com/legowiifun/GeometryDashStatDisplay"
	},
	"resources": {
			"sprites": [
					"resources/*.png"
			]
	}
}
